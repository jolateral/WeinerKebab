// Shared by PlayerController, JunctionTrigger, FanObstacle.
// Kept in its own file so any script can reference it without caring
// which other file "owns" it.
public enum Direction
{
    Up,
    Down,
    Left,
    Right
}
